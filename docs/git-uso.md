# Uso
- He instalado las dependencias con:
```
npm init -y
npm install jest
npm install jsdoc
```

- Ramas utilizadas:
```
main
deploy
feature/estructura
release
```

- Comandos usados:
```
git pull
git add nombre
git commit -m "Texto"
git push origin rama
```